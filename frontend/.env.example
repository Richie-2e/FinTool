# Base URL of the running FinTool backend (backend/main.py — no trailing slash).
# Copy this file to .env and set this to match how you're actually running
# the backend, e.g.:
#   uvicorn backend.main:app --port 8000   ->   http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
-------------------------------------------------------------------------

